name: CI

on:
  push:
    branches:
      - main
      - master
      - develop
  pull_request:
    branches:
      - main
      - master
      - develop

# Restrict permissions to minimum required for security
permissions:
  contents: read
  pull-requests: write  # Allow posting coverage comments

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'
          cache-dependency-path: 'requirements_test.txt'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements_test.txt
          pip install pytest-cov

      - name: Run tests with coverage
        run: |
          pytest --cov=custom_components.snapmaker --cov-report=xml --cov-report=term

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}

  lint:
    name: Code Quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install linting tools
        run: |
          python -m pip install --upgrade pip
          pip install black isort

      - name: Check code formatting with black
        run: |
          black --check custom_components/ tests/

      - name: Check import sorting with isort
        run: |
          isort --check-only custom_components/ tests/

  validate:
    name: Validate Home Assistant Integration
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Validate manifest
        run: |
          python3 -c "import json; json.load(open('custom_components/snapmaker/manifest.json'))"

      - name: Validate HACS
        run: |
          python3 -c "import json; json.load(open('hacs.json'))"

      - name: Check for stray Python files in repo root
        run: |
          # Python integration files belong in custom_components/snapmaker/, not the repo root.
          # Misplaced root-level .py files break pytest's import path resolution
          # (it walks up past the repo boundary when __init__.py exists at the root).
          stray=$(find . -maxdepth 1 -name "*.py")
          if [ -n "$stray" ]; then
            echo "ERROR: Found Python files in the repo root that do not belong here:"
            echo "$stray"
            echo ""
            echo "Integration code belongs in custom_components/snapmaker/"
            echo "Test support files belong in tests/"
            exit 1
          fi
